# Environment Variables for TTISA NTUT Website

## For Development (.env.local)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_APP_URL=http://localhost:5173

## For Production (Vercel Environment Variables)
# Set these in your Vercel dashboard:
# 1. Go to your project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add these variables:

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Application URL - IMPORTANT for domain changes
# This URL is used throughout the application for:
# - SEO meta tags and canonical URLs
# - OAuth redirect URLs for authentication  
# - Sitemap and robots.txt generation
# - Social media sharing URLs
# 
# Development:
# VITE_APP_URL=http://localhost:5173
# 
# Production (update this when changing domains):
VITE_APP_URL=https://ttisa-ntut.vercel.app

## Supabase Auth Configuration
# IMPORTANT: When changing VITE_APP_URL, update Supabase settings too!
# 1. Go to Supabase Dashboard > Authentication > URL Configuration
# 2. Set "Site URL" to your main domain
# 3. Add these URLs to "Redirect URLs" (replace with your actual domain):

# Site URL: https://ttisa-ntut.vercel.app
# Redirect URLs:
# - https://ttisa-ntut.vercel.app
# - https://ttisa-ntut.vercel.app/auth/callback
# - https://ttisa-ntut.vercel.app/update-password
# - http://localhost:5173 (for development)
# - http://localhost:5173/auth/callback (for development)  
# - http://localhost:5173/update-password (for development)